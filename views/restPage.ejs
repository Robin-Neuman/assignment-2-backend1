<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Restaurant</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="/script.js"></script>
    <script>

    </script>
</head>

<body>
    <div id="allCont">
    <form action="/home" method="GET">
        <button class="btn" type="submit">Home</button>
    </form>
    <div id="cont">        
            <form action="/editRest" method="POST">
                <div id="titleBox">
                <h1 id="restaurant"><%= name %></h1>
                <input type="text" value="<%= name %>" name="restPage" hidden>
                <button type="button" id="editBtn">Edit</button>
                <button type="submit" id="editSubmit" class="hidden">Submit</button>
            </div>
            </form>
        <div>
            <h3>Reviews:</h3>
        </div>
        <div id="reviews">
            <% for(let i=0; i < reviews.length; i++){ %>
            <div class="review">
                <p><span class="highlight"><%= reviews[i].username %> wrote:</span> <%= reviews[i].body %></p>

                <p><span class="highlight">Rating: </span><span id="test"><%= reviews[i].rating %>/5</span></p>

                <form action="/removeRev" method="POST">
                    <input type="text" value="<%= reviews[i].body %>" name="body" hidden>
                    <input type="text" value="<%= reviews[i].username %>" name="username" hidden>
                    <input type="text" value="<%= reviews[i].id %>" name="id" hidden>
                    <input type="text" value="<%= name %>" name="restPage" hidden>
                    <button type="submit">Delete</button>
                </form>
            </div>
            <% } %>
        </div>
        <h3>Commenting as <%= username %>:</h3>
        <form action="/addReview" method="POST">
            <input type="text" value="<%= username %>" name="username" hidden>
            <input type="text" value="<%= name %>" name="restName" hidden>
            <textarea type="text" name="body" placeholder="write your review here..." required></textarea>
            <h4>Rating: </h4>
            <select name="rating" required>
                <option value=""></option>
                <option value="1">1</option>
                <option value="2">2</option>
                <option value="3">3</option>
                <option value="4">4</option>
                <option value="5">5</option>
            </select>
            <button type="submit">Submit</button>
        </form>
    </div>
        <form action="/logout" method="POST">
            <button class="btn" type="submit">Log out</button>
        </form>
    </div>
</body>

</html>